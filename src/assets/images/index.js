const images = {
  logo: require('~/assets/images/logo.svg').default,
  rightArrow: require('~/assets/images/right-arrow.svg').default,
  leftArrow: require('~/assets/images/left-arrow.svg').default,
};

export default images;
